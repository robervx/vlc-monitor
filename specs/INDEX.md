# Índice de Specs — VLC Monitor

Antes de escribir código, mira aquí. No se implementa nada sin fila `Draft`/`Approved` con dependencias en `Implemented`. No reutilices un id ni lo borres al deprecarlo — márcalo `Deprecated` y enlaza la sucesora.

| id | Título | Fase | Estado | Depende de |
|---|---|---|---|---|
| `000` | Mapa base + geometría de distritos | F0 | **Implemented** — DoD completo (mapa, endpoint, servicio de geometría, tests). **v4 `Implemented` (2026-09-16):** quitado el resaltado de distrito al pasar el ratón/clic; el foco de distrito (spec 036) sigue funcionando solo vía el chip "Foco: X" | — |
| `001` | Capa de meteorología (Open-Meteo) + avisos oficiales (GVA Emergencias, scraping) | F1 | **Implemented (v4, 2026-09-16)** — Open-Meteo en producción desde v2. **v4 sustituye el plan de avisos AEMET (nunca activado, key obligatoria + web pública inservible para scraping — HTTP 500 y contenido por JS, ver spec §2) por scraping de `comunica.gva.es/es/emergencies-i-interior`** (GVA Emergencias e Interior, HTML sin JS, `robots.txt` permite bots de IA explícitamente). `src/services/avisos-meteo.ts` (9 tests con fragmento HTML real) + endpoint `GET /api/meteo/v1/avisos`, integrado en el motor de insights de spec 013 v7 (`aviso-oficial-meteo`) sin UI nueva. Verificado contra el dev server real y en navegador con la alerta naranja real del 15/09/2026 | `000` |
| `002` | Capa de calidad del aire | F1 | **Implemented** — Open-Meteo Air Quality en producción | `000` |
| `003` | Capa de movimiento de personas (mock) | Prototipo | **Implemented** — DoD completo, datos 100% sintéticos, badge "MOCK" siempre visible. **v3 `Implemented` (2026-09-16):** concentración por hotspots sobre los 689 monumentos falleros reales (`ScatterplotLayer` nuevo encima del choropleth), además de la densidad plana por distrito | `000` |
| `004` | Capa de tráfico en tiempo real | F1 | **Implemented** — v3: Geoportal ArcGIS en producción, 412 tramos, efecto de flujo animado (ritmo lento, excluye tramos cortados) trasladado aquí desde spec 022. **v4 `Implemented` (2026-09-16):** resaltado por severidad de los estados no-fluidos (ancho creciente + orden de dibujo encima de fluido) | `000` |
| `005` | Capa Valenbisi | F2 | **Implemented** — Geoportal ArcGIS en producción, 273 estaciones | `000` |
| `006` | Capa de aparcamiento | F2 | **Implemented** — Geoportal ArcGIS en producción, 23 parkings (13 sin dato de sensor, ver spec §7) | `000` |
| `007` | Capa EMT (bus) | F2 | Planned — fuente investigada 2026-08-18: paradas confirmadas (Geoportal, capa Trafico/MapServer/226), pero **llegadas en tiempo real sin API limpia** — el enlace `proximas_llegadas` de cada parada apunta a una página HTML (`emtvalencia.es/QR.php`) sin datos ni endpoint AJAX visible, no una API estructurada. No se implementa por no forzar scraping frágil de un endpoint no documentado (ver `CLAUDE.md` — fuentes deben tener cauce legal/técnico explícito). Reabrir si aparece una fuente confirmada (API oficial EMT, GTFS-RT). | `000` |
| `008` | Agenda y aglomeraciones previsibles (v1: Fallas) | F3.5 | **Implemented** — v1 acotada a Fallas en producción (689 monumentos, 462 carpas, zonas de movilidad reducida); agenda general queda como fast-follow (scraping, decisión pendiente de diseño) | `000` |
| `009` | Contexto mediático (RSS + Google News + Reddit) | F4 | **Implemented** — v5: 7 fuentes (Las Provincias, Valencia Plaza, 20minutos; Levante-EMV y Cadena SER vía Google News; Valencia Secreta/Bonita de ocio), resiliencia por fuente. **Filtro Valencia-ciudad estricto sobre TODAS las fuentes** (`filtro-ambito-ciudad.ts` + gazetteers) — descarta província/Comunitat/Venezuela + **política nacional/internacional** (`marcadoresFueraCiudad`) + crónica deportiva; `Valencia Plaza` ya no auto-confirma; bucket `general` visible, toggle "Ocio y deporte". `resumen` sin HTML/coletillas WordPress; dedup VA/ES del mismo medio (`dedup-noticias.ts`, conserva la versión ES). v5 retiró GDELT y À Punt. Registro compartido con spec 025. 276/276 tests, build y navegador con datos reales. Pendiente: Reddit (credenciales), sala de prensa del Ajuntament (fast-follow Playwright). **v6 `Implemented` (2026-09-16):** bucket "ocio y deporte" retirado — solo sobrevive el deporte de fútbol (marcadores de club/competición/estadio, gana incluso sobre señal positiva de ciudad); `categoria` pierde `'ocio'`; caducidad por ítem (24h) + "próxima actualización" en el panel | `000`, `023` |
| `010` | Pulso de Distrito (señal de proactividad por escenarios) | F3 | **Implemented (v4, 2026-09-16)** — el índice ponderado 0-100 de v3 se retira; catálogo de 3 escenarios de conjunción (`incidencia-sobre-trafico-denso` y `fallas-y-trafico` a vivo; `lluvia-inminente-sobre-trafico-denso` en modo sombra, `scripts/snapshot-pulso-sombra.ts` enganchado al cron horario de spec 017), evaluador consolidado `src/services/pulso-escenarios.ts` compartido con el motor de insights (spec 013), localización por calle + marcador en el mapa, nivel `seguimiento`/`prioritario`, histéresis anti-parpadeo (`cachePeek`/`cachePoke`), dedup por distrito, choropleth de 3 estados visuales. Aire fuera como disparador. Consumidores actualizados: `013` v6, `034` v4, `036` v2, `037` v3. 26 tests nuevos (346/346 en total), verificado en navegador contra datos reales | `001`, `002`, `004`, `008`, `016`, `026` |
| `011` | Densidad de movilidad agregada real | F6 | **Blocked** — requiere contrato comercial con proveedor de datos agregados y revisión de cumplimiento antes de poder pasar a Draft (ver `CLAUDE.md` §4) | `003` (sustituye al prototipo) |
| `012` | Geolocalización de usuario + "qué tengo más cerca" | F5 | **Implemented** — desbloqueada por ADR-002; sección "Cerca de mí" en el sidebar, ranking por distancia (tráfico/Valenbisi/aparcamiento), posición nunca persistida ni enviada a ningún endpoint. Verificado con typecheck/test (117/117) y en navegador | `000`, `004`, `005`, `006`, `019` |
| `013` | Motor de insights y alertas operativas | F3.6 | **Implemented (v3)** — 6 reglas (calor/frío extremo, aire malo, lluvia intensa, distrito crítico, viento fuerte v3), panel con alerta + borrador para copiar, sin envío automático ni destinatarios (diseño "avisa, no actúa" resuelto explícitamente, ver spec §0). **v4a `Implemented` (2026-09-09):** el "popup" — toast arriba a la derecha al aparecer un insight nuevo (nunca en la primera carga), apilado máx. 3, auto-cierre, clic → resalta el panel. **v4b `Implemented` (2026-09-09)**: disparador `trafico-empeora` (estado previo vía `cachePeek`/`cachePoke`, no dispara en frío), banda de aviso de calor a 35 °C, regla `lluvia-prevista` blanda. 284 tests. **v5 `Implemented` (2026-09-16):** alertas pasan a modal bloqueante (no toast), en cola si hay varias, sin autocierre; título de `trafico-empeora` nombra la calle del tramo más severo. **v6 `Implemented` (2026-09-16):** consumidor de spec 010 v4 — `distrito-critico` retirada, nueva `pulso-distrito` (agrupada por distrito, escenarios vivo+confirmado). **v7 `Implemented` (2026-09-16):** consumidor de spec 001 v4 — nueva regla `aviso-oficial-meteo` (avisos oficiales de la GVA por scraping), severidad por nivel, sin UI nueva. **v8 `Implemented` (2026-09-17):** bug real reportado por el usuario — `#insights-panel` crecía más que sus vecinas y rompía la alineación de `#info-panels`; corregido en la clase compartida `.info-panel` (tamaño fijo 280×280px + scroll interno), afecta a las 4 tarjetas fijas y a las 6 leyendas de capa | `001`, `002`, `010`, `016`, `004` |
| `014` | Sistema de notificaciones (push/email) sobre alertas | F3.6 | Planned — no redactada aún. Evaluar si se fusiona en el DoD de `008` en vez de ir como spec separada | `008` |
| `015` | Integración de rutas externas (Waze/Google Maps) por cortes | F7 | Planned — necesita investigación de viabilidad (programa Waze for Cities / convenio con el Ayuntamiento) antes de poder pasar a Draft, igual que `011` | `004`, `008` |
| `016` | Predicción meteorológica a corto plazo (nowcasting) | F4.5 | **Implemented (v3)** — Open-Meteo `hourly` en producción, panel "Próximas 4h" junto al de meteo actual. v3: panel movido a `src/ui/meteo-panel.ts` (mismo refactor que spec 001 v3) | `001` |
| `017` | Histórico y analítica de tráfico | F4.5 | **Implemented** — GitHub Actions cron cada 60 min, snapshots agregados versionados en el repo con compactación a diario pasados 30 días, panel con sparkline de las últimas 24h. **v4 (2026-09-10):** `SnapshotDistrito.porEstado?` — conteo de tramos por estado en el snapshot horario (prerrequisito de la calibración en sombra de spec 010 v4). Campo opcional/retrocompatible, rollup diario y endpoint de lectura sin cambios. 2 tests nuevos, script verificado contra la fuente, 305/305 | `004` |
| `018` | Acceso protegido (gate propio en middleware) + dominio propio | F5 | **Implemented** — `middleware.ts` (edge) + réplica local en `vite.config.ts`; usuario + PIN (PBKDF2 en `APP_USERS`), cookie firmada HMAC 30 días, rate-limit 5/15min, endpoints `api/auth/v1/{login,logout,estado}`, pantalla de login con marca IMC servida por el middleware, `npm run auth:hash`, logout en el sidebar. 224/224 tests, verificado con curl y en navegador. Pendiente solo: persistencia de sesión en PWA iOS (se cierra con `028`). Dominio: lo compra/apunta el usuario, no bloquea | `019` |
| `019` | Identidad de marca (Mirall, ex-"Intelligent City Monitor") + chasis de navegación (cabecera + sidebar plegable) | F0 | **Implemented** — v3: nombre corregido, sección Configuración funcional (elegir qué paneles se ven), `#info-panels` con wrap+scroll, reloj con fecha. v4: re-marca genérica (ADR-002). **v5 (2026-09-09):** pulido del sidebar — ocultar etiquetas al plegar (fix del sliver de letras) y cerrar el sidebar de escritorio al clic fuera (dejaba de tapar `#controls`). Verificado en navegador (escritorio + móvil), typecheck/test 276/276. **v6 `Implemented` (2026-09-16):** bug real corregido — abrir en escritorio y pasar a layout móvil dejaba Esc sin cerrar la hoja (`onCambioLayout` no reconciliaba el listener de `keydown` como sí hacía con `pointerdown`); el backdrop y el resto de cierres ya funcionaban bien | `000` |
| `020` | Grafo viario base (nodos + tramos), infraestructura reutilizable | F8 | **Implemented** — recorte de la Opción B de `ADR-002`, sin simulador. Overpass+TS (no Geofabrik/osmnx, ver spec §2), índice `rbush` (no H3). v3: el grafo (~9 MB) se sirve como estático del CDN (`/data/red-viaria-rodada.json`), no como función edge. v4 (paso 1 de la revisión del gemelo digital): sentido de circulación auditado y corregido (rotondas → unidireccional, `oneway=-1` invierte geometría), re-seed 13.233 tramos/9.201 nodos, `flechas-sentido.ts` + capa `?debug=grafo`. CDNCV verificado como fuente, resolución de nombre y de sentido pendiente (ver spec §7) | `000` |
| `021` | Motor de propuesta de cordón de seguridad por incidente | F8 | **Implemented (v4, 2026-09-16)** — DoD de software completo desde v2. v3 (paso 3 revisión gemelo digital): cortes de calle a mano + efecto en cadena de spec `031` sobre cerrados + cortes manuales, mostrando solo la propagación que se escapa del área de socorro; flechas de sentido. typecheck/test (241/241) y navegador. **v4: se retira el bloqueante de contraste con bomberos** — decisión de producto: es una primera aproximación editable para cuando llega la policía local primero, no sustituye el criterio de otros cuerpos, que ajustan después en su propio PMA | `020`, `019`, `031` |
| `022` | Simulador de cortes de calle ("¿qué pasa si cortamos estas calles?") | F8 | **Implemented** — v5: adopta el motor compartido de spec `031` (`simulacion-cortes.ts` borrado), tres categorías de resultado (sin entrada / sin salida / desvío forzado) + flechas de sentido; referencia = SCC principal (jubila el nodo único de Plaza del Ayuntamiento). v6 (paso 4): animación cualitativa del desvío — `reruta-corte.ts` calcula la ruta más corta que rodea cada corte y se anima el "flujo que cambia de dirección" (verde) sobre ella; la calle cortada no se anima. v7: fix móvil — flechas de sentido como icono SVG (no glifo) y solo sobre los tramos del análisis (no todo el viewport), que se veían "como líneas de error". Exclusión mutua con spec 021. typecheck/test (261/261) y navegador (incl. viewport móvil) | `020`, `019`, `031` |
| `023` | Geolocalización de contexto mediático por distrito (matching de texto, sin IA) | F4 | **Implemented** — v4: seed amplía `data/distritos-valencia.json` con los 88 barrios reales (alias/ambiguo curados), matching determinista en `src/services/geolocalizacion-texto.ts` (8 tests), panel de contexto mediático agrupado por distrito + "Valencia (general)" con chips. Verificado con typecheck/test (179/179), build y en navegador | `000`, `009` |
| `024` | Motor de insights v2 — correlación operativa (tráfico + Fallas + contexto mediático, sin modelo estadístico nuevo) | F8 | **Implemented** — v2: siguiente paso real de la línea de gemelo digital tras asesoría de ciencia de datos (2026-08-26); se descarta la Fase 1 de línea base/anomalías por falta de fuente de conteo continuo verificada, se implementa en su lugar correlación declarativa entre señales ya `Implemented` (3 reglas nuevas + enriquecimiento de `distrito` en Fallas + `fuenteSpec` como array + chips en UI). Regla de contexto mediático sigue gated tras `023` (aún `Draft`), no bloquea el resto. Verificado con typecheck/test (171/171), build y en navegador contra datos reales | `013`, `004`, `008`, `001`, `016`, `023` |
| `025` | Tendencia de términos del contexto mediático (frecuencia por hora/día) | F4 | **Implemented** — conteo de frecuencia determinista (sin modelo de lenguaje), reutiliza las cachés de la spec 009 (sin llamadas de red nuevas), chips de distrito vía spec 023. Verificado con typecheck/test (198/198), build y en navegador contra datos reales (5 ítems/hora vs 65/día) | `009` |
| `026` | Incidencias oficiales de vía pública (obras, cortes puntuales, festejos) | F3.5 | **Implemented** — 495 incidencias activas en producción (279 obras, 216 incidencias, 0 festejos), point-in-polygon exacto (spec 000), capa visible solo a zoom de calle (primer uso real de `zoomMinimo`), aviso visible de que las fechas son vigencia de permiso. Verificado con typecheck/test (186/186), build y en navegador | `000` |
| `027` | Agenda general de eventos culturales (scraping resiliente) | F3.5 | **Implemented (v4, 2026-09-17)** — `src/services/agenda-eventos.ts` (parsers puros + `estructuraSospechosa`), `scripts/scrape-agenda-eventos.ts` (Playwright) + `.github/workflows/agenda-eventos-cron.yml` (cron 6h, primera ejecución real de CI verificada el 2026-09-17), endpoint `GET /api/agenda/v1/eventos`, panel agrupado por distrito con aviso persistente de scraping. **v4:** eventos con impacto en vía pública (fútbol de Valencia CF/Levante UD como local, Roig Arena, carreras FDM) — 4 fuentes nuevas verificadas en vivo, ninguna necesita Playwright (HTML/JSON plano a `fetch()`), `src/services/agenda-impacto-vial.ts` (10 tests), sección destacada "⚠ Impacto en vía pública" en el panel con chip distintivo. 377/377 tests, `typecheck`/`build` verdes, verificado en navegador con datos reales (19 eventos de impacto) | `000`, `023` |
| `028` | PWA instalable + shell offline | F5 | **Implemented** — `vite-plugin-pwa` (manifest + SW `generateSW`, `registerType: prompt`), iconos generados (`npm run marca`), navegación NetworkFirst (respeta el gate 018), SWR para datos, CacheFirst tiles, NetworkOnly `/api/auth/*`, interceptor de 401 + toast de actualización en `src/pwa.ts`. Build/typecheck/test verdes; runtime del SW pendiente de verificar en el primer despliegue (el navegador de la sesión no registra SW) | `019`, `018` |
| `029` | UX móvil: layout táctil del chasis + detección automática | F5 | **Implemented** — detección reactiva escritorio/móvil (`matchMedia`, sin UA sniffing, override manual persistido) + chasis táctil ≤ 640 px: sidebar como hoja (`transform`, FAB externo, cierre por ✕/Esc/swipe/backdrop, trampa de foco), `#info-panels` como bottom sheet arrastrable de 3 estados con reparentado de `#controls`/`#media-panel`/`#tendencia-panel`, cabecera compacta, `safe-area`, targets ≥ 44 px. Escritorio sin cambios. 231/231 tests, verificado en navegador; safe-area con notch y Lighthouse pendientes del despliegue | `019`, `028` |
| `031` | Motor de propagación dirigida de cortes de calle (compartido 021/022) | F8 | **Implemented** — motor puro (`propagacion-corte.ts`, 9 tests): dado un conjunto de calles cortadas, calcula por alcanzabilidad dirigida qué tramos quedan `sinEntrada` / `sinSalida` / `aislado` y qué vías abiertas desembocan en un corte (`desvioForzado`), como diff contra línea base. Referencia = SCC principal (Kosaraju iterativo, 95,4% de nodos). Precálculo memoizado en `grafo-viario-cliente.ts` (~110ms una vez, ~35-75ms por corte). Verificado contra el grafo real (Av. del Port, Ciutat Vella, Eixample). Consumido por 021 v3 y 022 v5/v6 | `020` |
| `032` | Reconciliación de la capa de tráfico real (spec 004) con el grafo viario (spec 020) | F8 | **Implemented (v2, 2026-09-16)** — `src/services/reconciliacion-trafico-grafo.ts`: emparejamiento por muestreo de puntos + índice espacial de spec 020, nombre normalizado + solape geométrico (buffer 20 m). Verificado contra datos reales: **99,5 % de cobertura geométrica** (410/412 tramos), confianza mayoritariamente `media` (coincidencia de nombre solo confirmada en 3,9 % — limitación de nomenclátor ya conocida, spec 020 §7). Sin artefacto pre-calculado ni consumidor todavía (021/022/031 usarlo queda para spec futura). 9 tests nuevos (355/355 en total) | `004`, `020` |
| `030` | Re-marca genérica + repo público | F5 | **Implemented** — ADR-002: se retira el logo del proyecto y la identidad institucional; logo → placeholder neutro generado (`npm run marca`, Node puro), nombre/tagline/pie en `src/config/marca.ts`, gate de acceso (018) pasa a **opcional** (sin `AUTH_SECRET` la app va abierta), `LICENSE` MIT, README para audiencia pública, `CLAUDE.md` §1 reescrito (§4 intacto). typecheck/test/build verdes, verificado en navegador (abierto y con gate). **v3 (2026-09-04):** README reescrito como pieza de portfolio (ES + resumen EN, demo en vivo, capturas en `docs/capturas/`), nuevo `docs/FUENTES_Y_LICENCIAS.md` (inventario fuente/licencia/atribución por capa) y `docs/PRESENTACION_LINKEDIN.md`; sin cambios de código. **v4 `Implemented` (2026-09-16):** rebranding a **Mirall** (`docs/decisiones/ADR-004-rebranding-mirall.md`) — nombre + `MARCA.descriptor` ("Urban Intelligence Platform") en cabecera con tipografía propia (Space Grotesk), `MARCA.tagline` (ES) queda solo para presentación externa; manifest de PWA parametrizado desde `MARCA`; "VLC Monitor" (nombre de trabajo interno) sustituido en atribuciones de insights/histórico/tendencia | `019`, `018` |
| `033` | Jerarquía de capas en el selector | F5 | **Implemented (v4, 2026-09-16)** — v2: selector en dos grupos, "Prioritarias" y "Contexto e informativas" con `<details>` plegable, contador, botón "Vista operativa". Campo `grupo` en `map-layer-definitions.ts`. **v4 (consumidor de spec `040`):** cámaras/contexto mediático/tendencia salen del selector (pasan a contenido fijo de `/inteligencia`, ya no son capas); "Prioritarias" vuelve a tráfico/Pulso de Distrito/incidencias de vía pública, "Contexto e informativas" queda en 4 filas. typecheck/test/build verdes, verificado en navegador | `019` |
| `034` | Dashboard de indicadores (KPIs) | F3.6 | **Implemented** — v2: tira fija arriba-centro con 5 KPIs (Temp, Aire, Tráfico, Pulso, Alertas), agregación **en cliente** sobre datos que los paneles ya fetchean (`src/ui/dashboard-kpis.ts`, patrón de `estado-frescura.ts`) — sin endpoint nuevo. Color por tono (umbrales importados de `insights.ts`); chip de Tráfico/Pulso clicable → activa la capa. typecheck/test 276/276, verificado en navegador. **v3 `Implemented` (2026-09-16):** quitado el KPI de temperatura (duplicaba el panel de meteo); la tira pasa de 5 a 4 chips. **v4 `Implemented` (2026-09-16):** el chip "Pulso" pasa a leer `nivel` (spec 010 v4) en vez del `indice`/`categoria` retirados | `004`, `001`, `002`, `010`, `013` |
| `035` | Frescura global + mini-leyendas | F5 | **Implemented** — rollup de frescura en la cabecera (verde/ámbar/gris + "N fuentes · hace N min", `src/ui/estado-frescura.ts`) y leyendas de capa que arrancan colapsadas a una línea y se expanden al hover/foco/tap. typecheck/test 276/276. **v2 `Implemented` (2026-09-16):** quitado el auto-expandir-con-hover en las 6 leyendas, ahora solo con clic/tap | `019` |
| `036` | Foco de distrito | F5 | **Implemented** — clic en un distrito del mapa → chip "Foco: <distrito>", el contexto mediático se filtra a ese distrito (+ buckets de ciudad) y el Pulso antepone su nivel. Store `src/ui/foco-distrito.ts`, sin persistencia. typecheck/test 284/284. **v2 `Implemented` (2026-09-16):** consumidor de spec 010 v4 — antepone `nivel`/escenarios en vez de `índice`; el resaltado visual del distrito al clic se retiró en spec 000 v4 (el foco en sí sigue igual) | `000`, `009`, `010`, `023` |
| `037` | Glosario / cómo funciona (sección de lectura en el sidebar) | F5 | **Implemented** — `src/ui/glosario.ts`: 8 entradas en acordeón (Pulso, capas del mapa, Prioritarias vs. Contexto, "en vivo"/"no actualizado", alertas e insights, MOCK, foco de distrito, fuentes y licencias). Los números del Pulso y de las alertas se **leen de las constantes** (`PESOS_PULSO`/`UMBRALES_CATEGORIA_PULSO` de `pulso-distrito.ts`, umbrales de `insights.ts`); la lista de capas se deriva de `LAYER_REGISTRY` + `META_CAPAS` con guardarraíl de cobertura. Entrada en `SIDEBAR_REGISTRY`, CSS `.glosario-*`. 10 tests, typecheck/test (303)/build verdes, verificado en navegador. Revisión de contenido `acda93e` (amplificación ×2,5 del tráfico, colores reales de "en vivo", licencias por fuente). **v3 `Implemented` (2026-09-16):** entradas "Pulso de Distrito" y "Alertas e insights" reescritas para spec 010 v4 / spec 013 v5-v6 | `019`, `010` |

| `038` | Cámaras urbanas en vivo (embeds de terceros, sin captura propia) | F9 | **Implemented (v7, 2026-09-16)** — v6: refactor puro, panel extraído de `main.ts` a `src/ui/camaras-panel.ts`. Cámara de YouTube **retirada** (el canal amateur no emitía); **4 cámaras** de la Xarxa de Webcams de Turisme CV vía `dashjs` (`ADR-003`, personales): Plaça de l'Ajuntament, Platja de Les Arenes/El Cabanyal, Jardín del Turia y El Saler. `meteo365.es` investigada y **descartada** (hotlinking + prohibición legal). Recuadro a 16:9 real, aviso explícito de cámara caída. **v7 — DoD de V1:** el selector de capas oculta del todo la entrada "Cámaras en vivo" sin `VITE_PERSONAL_CAMARA_TURISME_CV` (antes se mostraba vacía), verificado arrancando el dev server con y sin la variable; fiabilidad del autoplay cerrada como riesgo aceptado con mitigación (controles nativos siempre visibles) — sin navegador real conectado en esta sesión para la confirmación final. typecheck/test (357/357)/build verdes | — |
| `039` | Actualidad institucional en redes (widgets oficiales Facebook/X) | F9 | **Implemented (v5, 2026-09-17)** — 13 entidades como fichas en acordeón, carga diferida por entidad, selector "Elegir qué cuentas leer" con preferencia persistida (`imc:entidades-redes-ocultas`). **v4:** sale del sidebar a un panel propio (`#actualidad-redes-panel`) dentro de `/inteligencia`. **v5 — bug real reportado por el usuario** ("el widget de X no funciona"): sin `onerror` en el `<script>` del SDK, un bloqueador de anuncios dejaba la promesa colgada para siempre y el fallback (programado después de esperarla) nunca se disparaba — ficha en blanco sin aviso. Corregido: `onerror` + fallback programado antes de esperar el SDK. Verificado simulando el bloqueo real. typecheck/test (367/367)/build verdes | `019`, `040` |

| `040` | Reestructuración en dos vistas (mapa operativo / hub de inteligencia) | F10 | **Implemented (2026-09-16)** — router por hash (`src/ui/router.ts`) + nav en la cabecera, sin dependencia nueva. `/inteligencia` reúne cámaras/contexto mediático/actualidad institucional/tendencia/agenda (estas dos últimas no estaban en el alcance original, confirmadas con el usuario al implementar) como contenido fijo, sin capas que se enciendan/apaguen. Visibilidad por vista sin reparentar nodos (ortogonal al bottom sheet móvil de spec 029). Bug real encontrado y corregido: `writeStateToUrl` borraba el hash de vista en cada movimiento de mapa. Verificado en navegador (escritorio y móvil) con datos reales, sin duplicar paneles tras varios cambios de vista. typecheck/test (357/357)/build verdes | `019`, `033` |
| `041` | Panel de apoyo a decisión operativa (cruce de señales existentes) | F10 | **Implemented (2026-09-16)** — traduce a texto los escenarios ya calculados por el evaluador consolidado de `010` (sin recalcular tráfico/incidencias/lluvia por su cuenta), sin modelo estadístico nuevo (mismo principio que `024`); sugerencias siempre en condicional, nunca una acción ejecutable (`CLAUDE.md` §4). "Ver en el mapa" centra la única instancia de MapLibre y cambia a `/mapa`. `021` (cordón) queda fuera del cruce automático — su estado es de sesión de cliente, no una caché de servidor. Verificado en navegador (escritorio/móvil); sin conjunción real durante la verificación, caso con datos cubierto por 7 tests con fixtures + inyección visual, documentado como tal. typecheck/test (364/364)/build verdes | `010`, `016`, `026`, `040` |
| `042` | Página de protocolos de actuación | F10 | **Implemented (v2, 2026-09-17)** — `src/config/protocolos-actuacion.ts`, panel dentro de `/inteligencia`, fecha de última revisión siempre visible. Contenido de "Lluvias intensas" (recomendaciones generales de protección civil, sin fuente oficial citada) **confirmado por el usuario el 2026-09-17, sin cambios**. typecheck/test (367/367)/build verdes, verificado en navegador (escritorio y móvil) | `040` |
| `043` | Cámaras urbanas externas (red viaria que rodea Valencia) | — | **Draft** — creación 2026-09-17, segundo punto de la tanda de trabajo post-V1 (`docs/03_PLAN_POST_V1.md`). Bloque separado de las cámaras internas de spec `038`, inspirado por el usuario en `livetrafik.com` (agregador, no la fuente primaria — hipótesis: red de cámaras de la DGT, sin confirmar). Due-diligence ligera hecha, investigación real pendiente | `038`, `040` |
| `044` | Panel de emergencia meteorológica avanzada (lluvia/viento por zona, pluviómetros, altimetría) | — | **Draft** — creación 2026-09-17, tercer punto de la tanda post-V1. 4 sub-señales con fuentes distintas (candidatos: Open-Meteo multi-punto, SAIH Júcar, IGN) — muy probable que se divida en varias specs al investigar en profundidad (ver spec §7). "Capacidad de absorción" sin fuente identificada todavía | `001`, `040` |
| `045` | Panel de síntesis con IA (insights y recomendaciones con guardrails) | — | **Draft** — creación 2026-09-17, cuarto y último punto de la tanda post-V1, a petición explícita del usuario de **analizar antes de implementar**. Rompe deliberadamente el principio "sin modelo estadístico/IA nuevo" de `010`/`013`/`024`/`041` — bloqueante explícito de decisión de producto/ADR (coste, proveedor, fiabilidad) antes de `Approved`, mismo peso que tuvo el bloqueante de contenido de spec `042` | `013`, `041`, `040` |

## Leyenda de estados


`Planned` (idea situada en el roadmap, sin spec redactada) → `Draft` (spec redactada, contrato de datos aún sin verificar/implementar) → `Approved` (contrato verificado, lista para implementar) → `Implemented` (en producción, cumple su Definition of Done) → `Deprecated` (sustituida, se conserva por trazabilidad).

`Blocked` es un estado especial: no puede avanzar a `Draft` hasta que se cumpla la condición indicada explícitamente en la fila.
