// GET /api/geo/v1/distritos — endpoint definido en specs/000-mapa-base-distritos.md §4.
// Sirve el GeoJSON de distritos desde caché/asset estático versionado — nunca
// llama a la fuente externa en el momento de la petición del usuario.
//
// Estado: stub — implementar tras cerrar la verificación manual pendiente en
// la spec 000.
export {};
